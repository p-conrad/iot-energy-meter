## SECTION=wago_specific_dal

config IOT_ENERGY_METER
	bool
	select DAL
	select PROTOBUF_C
	select PAHO_MQTT_C
	prompt "IoT Energy Meter"
	help
	  Install the IoT energy meter tool for reading energy data and sending them via MQTT
